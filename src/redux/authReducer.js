let initialState = {
  userId: null,
  email: null,
  login: null,
  isAuth: true, // change default to false
};

const authReducer = (state = initialState, action) => {
  switch (action.type) {
    

    default:
      return state;
  }
};

export default authReducer;